# 🚀 دليل النشر - منصة Wonderland للتجارة الإلكترونية

## المتطلبات الأساسية
- حساب GitHub (مجاني)
- حساب Render.com (مجاني)
- Git مثبت على جهازك

## الخطوة 1️⃣: رفع المشروع إلى GitHub

### 1.1 إنشاء مستودع جديد على GitHub
1. اذهب إلى [github.com/new](https://github.com/new)
2. أنشئ مستودع باسم `wonderland`
3. **لا تحدد** "Initialize with README" (لديك واحد بالفعل)
4. اضغط "Create repository"

### 1.2 رفع الكود
```bash
git remote add origin https://github.com/YOUR_USERNAME/wonderland.git
git branch -M main
git push -u origin main
```

استبدل `YOUR_USERNAME` باسم مستخدمك على GitHub.

## الخطوة 2️⃣: النشر على Render

### 2.1 ربط GitHub بـ Render
1. اذهب إلى [render.com](https://render.com)
2. سجل دخول أو إنشاء حساب عبر GitHub
3. اضغط "New +" → "Web Service"
4. اختر "Connect a repository" → اختر `wonderland`
5. ملء الإعدادات:
   - **Name**: `wonderland`
   - **Environment**: Node
   - **Region**: Oregon (أو الأقرب إليك)
   - **Branch**: main
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm run start`
   - **Instance Type**: Free

### 2.2 متغيرات البيئة
إذا كان لديك متغيرات بيئة حساسة:
1. في لوحة Render، اذهب إلى "Environment"
2. أضف المتغيرات المطلوبة
3. هذا المشروع عادة لا يحتاج متغيرات في البداية

### 2.3 النشر
1. اضغط "Create Web Service"
2. Render سينشر التطبيق تلقائياً
3. انتظر اكتمال النشر (5-10 دقائق)
4. التطبيق سيكون متاحاً على: `https://wonderland.onrender.com`

## الخطوة 3️⃣: نطاق مخصص (اختياري)

إذا أردت استخدام نطاق خاص بك:
1. في لوحة Render، اذهب إلى "Custom Domains"
2. أضف نطاقك
3. اتبع تعليمات إعدادات DNS
4. قد يستغرق التفعيل 24-48 ساعة

## الخطوة 4️⃣: النشر التلقائي

عند الاتصال بـ GitHub:
- كل دفعة (push) إلى `main` تنشئ نشراً تلقائياً
- يمكنك التحقق من حالة النشر في لوحة Render
- يمكنك العودة للإصدارات السابقة إذا حدث خطأ

## 🔒 معلومات الأمان

### المعلومات الحساسة
- **لا تكتب أبداً** المفاتيح السرية أو كلمات المرور في GitHub
- استخدم `.env.local` للعمل المحلي فقط (ليس في Git)
- استخدم لوحة Render لحفظ البيانات السرية الإنتاجية
- لا تكتب `.env` أو `.env.local` في Git

### المعلومات الآمنة للنشر
- ملفات الكود
- ملفات الإعدادات (بدون أسرار)
- `.env.example` (كقالب فقط)
- الملفات العامة
- المكتبات (package.json)

## 📊 مراقبة التطبيق

### عرض السجلات
1. اذهب إلى لوحة خدمتك في Render
2. اضغط على تبويب "Logs"
3. شاهد المخرجات الحية
4. مفيد لتصحيح أخطاء النشر

## 🆘 حل المشاكل

### فشل البناء
- تحقق من السجلات في Render
- تأكد أن `npm run build` يعمل محلياً
- تحقق من جميع المكتبات في `package.json`

### التطبيق يتعطل
- تحقق من السجلات في Render
- تأكد أن `npm run start` يعمل محلياً
- تأكد من توافق Node.js (14 أو أحدث)

## 📝 الخطوات التالية

بعد النشر:
1. اختبر جميع الميزات على الرابط الحي
2. راقب السجلات للأخطاء
3. ضع علامات مرجعية مهمة

## 🔄 تحديث التطبيق

لتحديث التطبيق:
1. اعمل تغييرات محلياً
2. احفظ: `git commit -m "رسالتك"`
3. ارفع: `git push origin main`
4. Render ينشر تلقائياً
5. راقب التقدم في لوحة Render

---

**نشر سعيد!** 🎉
