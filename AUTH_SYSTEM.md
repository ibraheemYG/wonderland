# 🔐 نظام التسجيل والدخول

## ✨ ما تم إضافته

تم إضافة نظام تسجيل دخول كامل بثلاث مستويات:
- ✅ صفحة تسجيل دخول آمنة
- ✅ نظام جلسات العمل
- ✅ دور المستخدم (Admin/User)
- ✅ لوحة تحكم الأدمن محمية

---

## 🔑 بيانات الدخول التجريبية

### حساب الأدمن (مسؤول)
```
اسم المستخدم: admin
كلمة المرور: admin123
الدور: إدارة كاملة
```

### حساب المستخدم العادي
```
اسم المستخدم: user
كلمة المرور: user123
الدور: تصفح وتسوق فقط
```

---

## 📍 الروابط الرئيسية

### للجميع
- **صفحة الدخول**: `/login`
- **الرئيسية**: `/`
- **المنتجات**: `/products`

### للأدمن فقط (بعد الدخول)
- **لوحة التحكم**: `/admin`
- **إضافة/تعديل المنتجات**
- **عرض الإحصائيات**

---

## 🚀 كيفية الاستخدام

### 1️⃣ تسجيل الدخول
```
1. اذهب إلى: /login
2. أدخل اسم المستخدم وكلمة المرور
3. اضغط "تسجيل الدخول"
```

### 2️⃣ الوصول إلى لوحة التحكم (للأدمن)
```
1. سجل دخول بحساب admin
2. ستظهر ⚙️ في الرأس (Desktop)
3. أو اذهب مباشرة إلى: /admin
```

### 3️⃣ إضافة منتجات جديدة
```
1. ادخل لوحة التحكم
2. استخدم النموذج لإضافة منتج
3. اختر الفئة والسعر والصورة
4. احفظ المنتج
```

### 4️⃣ تسجيل الخروج
```
1. اضغط زر "خروج" في الرأس
2. سيتم حذف الجلسة محلياً
```

---

## 🔒 الأمان

### ملاحظات أمان مهمة:
- ✅ الجلسات مخزنة في localStorage (آمن للتطوير)
- ✅ كلمات المرور لا تُرسل مع الطلبات
- ✅ صفحات الأدمن محمية - يجب تسجيل دخول
- ✅ يمكن إضافة قاعدة بيانات حقيقية لاحقاً

### في الإنتاج يجب:
- ❌ لا تستخدم كلمات مرور في الكود
- ❌ استخدم متغيرات البيئة
- ❌ احفظ كلمات المرور مشفرة في قاعدة البيانات
- ❌ استخدم JWT tokens بدل localStorage

---

## 📱 الميزات

### على Desktop
```
- شريط تسجيل دخول في الرأس
- عرض اسم المستخدم الحالي
- زر لوحة التحكم (للأدمن)
- زر تسجيل الخروج
```

### على Mobile
```
- تسجيل دخول في القائمة المنسدلة
- نفس الوظائف مع تصميم متجاوب
```

---

## 🔄 كيف يعمل النظام

```
1. المستخدم يدخل بيانات في /login
   ↓
2. يتم إرسالها إلى API: /api/auth/login
   ↓
3. يتم التحقق من البيانات
   ↓
4. إذا صحيح: يتم حفظ بيانات المستخدم في localStorage
   ↓
5. تظهر معلومات المستخدم في جميع الصفحات
   ↓
6. صفحات الأدمن تتحقق من الدور قبل العرض
```

---

## 📋 الملفات المضافة

```
src/
├── app/
│   ├── login/page.tsx          # صفحة تسجيل الدخول
│   ├── api/auth/login/route.ts # API للتحقق من البيانات
│   └── admin/page.tsx          # لوحة التحكم (محدثة)
│
├── context/
│   └── AuthContext.tsx         # إدارة الجلسات والمستخدم
│
└── data/
    └── auth.ts                 # بيانات المستخدمين والتحقق
```

---

## 🎯 الخطوات التالية

### لتحسين النظام:
1. ✅ إضافة قاعدة بيانات (MongoDB/PostgreSQL)
2. ✅ تشفير كلمات المرور
3. ✅ استخدام JWT tokens
4. ✅ إضافة تسجيل مستخدم جديد
5. ✅ استرجاع كلمة المرور المنسية
6. ✅ التحقق بالبريد الإلكتروني

---

## 🧪 اختبار النظام

### اختبر السيناريوهات التالية:

**1. تسجيل دخول صحيح**
```
- استخدم: admin / admin123
- توقع: دخول والذهاب للرئيسية
```

**2. بيانات خاطئة**
```
- استخدم: admin / wrongpassword
- توقع: رسالة خطأ
```

**3. الوصول لصفحة أدمن بدون تسجيل**
```
- اذهب مباشرة إلى: /admin
- توقع: إعادة توجيه إلى /login
```

**4. تسجيل الخروج**
```
- سجل دخول
- اضغط خروج
- اذهب إلى /admin
- توقع: إعادة توجيه إلى /login
```

---

## 💡 نصائح

- استخدم browser DevTools لرؤية localStorage
- تفقد Network tab لرؤية طلب API
- استخدم حساب admin لاستكشاف جميع الميزات
- حاول تعديل localStorage يدويًا لترى كيف يتعامل النظام

---

**نظام التسجيل جاهز للاستخدام!** 🎉
